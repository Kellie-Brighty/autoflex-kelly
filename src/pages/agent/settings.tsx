import AgentLayout from "@/layouts/agent/index.layout";
import React from "react";

const settings = () => {
  return (
    <AgentLayout title="Settings" caption="Manage your settings">
      settings
    </AgentLayout>
  );
};

export default settings;
